PROC swap(char &x, char &y) {
   char t = x; x = y; y = t;
}

PROGRAM lib() {
   char str[100];
   int i, l;

   READ_STRING(100, str);

   l = strlen(str);
   FOR (i, 0 TO l / 2)
      swap(str[i], str[l-1 - i]);

   WRITELN(str);
}
